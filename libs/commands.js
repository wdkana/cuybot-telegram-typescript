const commands = {
    quake: /^!quake$/,
    profile: /^!profile$/,
    quote: /^!quote$/,
    news: /^!news$/,
    help: /^!help$/,
    followme: /^!followme(.+)/,
    avatar: /^!avatar(.+)/,
}

module.exports = commands